<?xml version="1.0" encoding="utf-8"?><!-- res/drawable/placeholder_image.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#E0E0E0" /> <!-- Light gray -->
            <corners android:radius="8dp" /> <!-- Optional: Rounded corners -->
        </shape>
    </item>
    <!-- Centered loading icon or text -->
    <item
        android:width="48dp"
        android:height="48dp"
        android:gravity="center">
        <shape android:shape="oval">
            <solid android:color="#B0B0B0" /> <!-- Medium gray -->
            <size
                android:width="24dp"
                android:height="24dp" />
        </shape>
    </item>
    <!-- Optional: Add "Loading" text -->
    <item
        android:drawable="@android:drawable/ic_menu_rotate"
        android:gravity="center"
        android:tint="#757575" /> <!-- Dark gray -->
</layer-list>